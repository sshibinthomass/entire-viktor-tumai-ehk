.env
.venv/
env/

# licensed dataset: challenge use only, NO redistribution — never commit any of it
export/
export_linked/
*.tar.gz
*.zip

# derived artifacts that embed dataset text (previews, TF-IDF vocabulary, cached text)
dashboard/previews.js
results/exp_cache.npz
results/frozen_*.pkl

__pycache__/
catboost_info/
outputs/

# per-dataset lab files: previews hold verbatim task text (see dashboard/previews.js)
dashboard/data/*.previews.js

# browser uploads staged by scripts/lab_server.py (licensed dataset)
uploads/
